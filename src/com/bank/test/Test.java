package com.bank.test;

/*
import java.util.ArrayList;
import java.util.Iterator;

import com.bank.client.Client;
import com.bank.logic.Account;
import com.bank.logic.Agency;
import com.bank.logic.Customer;
import com.bank.logic.Operation;
*/

public class Test {

	public static void main(String[] args) {
		/*
		Client client = new Client(); 
		//Agency ag = new Agency("Val", "Dakar");
		Customer cus = new Customer("Leye", "Mohamed", 9);
		//Account act = new Account("AAAAAAAAAZ","Epargne","CR", 23466960,6);
		//Operation op =  new Operation("Credit","12/12/14", 123455,"CR", "AAAAAAAAAA");
		
		//client.call().createAgency(ag);
		//client.call().createCustomer(cus);
		//client.call().createAccount(act);
		//client.call().createOperation(op);
		
		Customer cus2 =  new Customer(cus);
		//Important : Specificate the id to use
		cus2.setCusID(5);
		cus2.setCusFirstname("Kurusan");
		client.call().deleteOrUpdateCustomer("update", cus2, "cusID");
		
		//Deleting something
		//Search what to delete by looking fetching on agName
		//client.call().deleteOrUpdateAgency("delete", ag, agName);
		
		//ArrayList<Operation> list = client.call().getOperations();
		/*if(list == null) System.out.println("Empty list");
		else {
			Iterator<Operation> it = list.iterator();
			while(it.hasNext()){
				System.out.println(it.next().getOpLib().toString());
			}
		}
		
		
		try {
			Thread.sleep(10000);
		} catch (InterruptedException e) {
			e.printStackTrace();
		}
		
		
		*/
		
	}

}
